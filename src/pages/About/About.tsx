import classes from './about.module.css';

export const About = () => {
    return (
        <div className={classes.wrapper}>
            <div className={classes.title}>
                This is main page!
            </div>
        </div>
    );
};
